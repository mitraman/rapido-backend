swagger: "2.0"
info:
  title: test-title
  description: project description
  version: rapido-sketch
paths:
  /somepath/{paramsegment}/get-node:
    get:
      description: auto generated by Rapido
      produces:
        - application/json
      consumes:
        - application/json
      parameters:
        - name: paramsegment
          in: path
          type: string
          required: true
        - name: /get-node.get.queryParams
          in: query
          type: string
        - name: request body
          in: body
          schema:
            type: string
            description: Rapido was unable to parse the JSON request body from this sketch node
      responses:
        200:
          description: auto generated by Rapido
          examples:
            text/plain: /get-node.get.response.body
          schema:
            type: string

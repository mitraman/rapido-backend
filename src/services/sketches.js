"use strict";

const EventStore = require('../event/EventStore.js');
const winston = require('winston');


let Sketches = function () {
    // Subscribe to event data
};

Sketches.prototype.addNode = function(newNode) {
  // Record the addition of the node in the event store

}


module.exports = new Sketches();
